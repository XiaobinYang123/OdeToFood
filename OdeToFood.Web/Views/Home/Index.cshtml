@model IEnumerable<OdeToFood.Data.Models.Restaurant>

@{
    ViewBag.Title = "Home Page";
}

    <div class="jumbotron">
        <h1>ASP.NET</h1>
        <p>Hello, WOrld!ssssss!</p>
        <!--
        @DateTime.Now.Year
        -->
        @*123*@
    </div>
        <div>
            @Model.Count() number of restaurants
        </div>
         @*
        <ul>
            @foreach (var restaurant in Model)
            {
                <li>@restaurant.Name</li>
            }
        </ul>
        *@
 

